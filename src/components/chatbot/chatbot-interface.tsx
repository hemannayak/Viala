'use client'

// Stub component — ChatbotInterface is not yet implemented in Phase 1.
export function ChatbotInterface({
  isOpen,
  onClose,
}: {
  isOpen: boolean
  onClose: () => void
}) {
  if (!isOpen) return null
  return null
}
